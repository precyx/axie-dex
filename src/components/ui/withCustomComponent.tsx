import React from "react";
/*

interface CustomComponentProps {
	CustomComponent:any,
	children:any,
}
interface CustomComponentState {}

export const withCustomComponent:React.FC<CustomComponentProps> = (props:CustomComponentProps) => {
	const {CustomComponent, children} = props;
	return (
		<CustomComponent {...props}>
			{children}
		</CustomComponent>
	)
}*/